{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":6,"timestamp":1786210150,"nonce":"3n6UPmcX9zsBgoPjxQigcjPvzdI=","target":"78d85539c07ef0a35c145a40558e83095be8a59667f5ffe3522983cecc3d041d","message":"**What is wrong.** SPEC §L.8.1 specifies four drift gates, \"all in CI\". Three do\nnot exist:\n\n- **G1** Go DTO → OpenAPI (`test/contract/dto_schema_test.go`): the directory\n  holds only a `doc.go`.\n- **G2** running server → OpenAPI via schemathesis: no schemathesis anywhere, no\n  CI step.\n- **G4** OpenAPI → generated valibot validators\n  (`web/src/api/generated/validators.ts`): the file does not exist and there is no\n  `gen:validators` script, so every valibot schema in `web/src` is a hand-written\n  one of the kind §L.8.1 forbids.\n\nG3 (TS client freshness), the AC-49 vocabulary lint and `TestValidatorMatchesDDL`\nare real and do run in CI; `README.md:100-102` states the three gaps honestly.\n\n**Why it matters.** The eight contract divergences the audit found were all caught\nby a human doing by hand what G1 and G2 automate, and that human is not coming\nback. Without G1 the Go DTOs and `openapi.yaml` drift the moment someone adds a\nfield; without G2 nothing checks that the server's actual responses match; without\nG4 the frontend validates against schemas someone typed, which is how\n`PoliciesSection.tsx` came to offer a reason the server rejects.\n\n**Where.** `test/contract/`, `.github/workflows/ci.yml`, `web/package.json`,\nSPEC §L.8.1, `README.md:100-102`, `docs/ORCHESTRATION.md:99-100`.\n\n**Done when.** All three gates exist, run in `.github/workflows/ci.yml`, and have\neach been demonstrated to fail against a deliberately planted drift before being\ndeclared done — the same standard the depguard rules were held to.","files":null}]}