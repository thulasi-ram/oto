{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1787109370,"nonce":"XgGxQO+KE342y/X6TrkKd2rceHc=","message":"VERIFIED DONE (premise now stale; closing on the owner's instruction).\n\n`QueueReconcile` is 8, not 2, in all three places that must agree: `DefaultQueueWorkers` (internal/platform/jobs/client.go), SPEC §G.3's `reconcile` rows, and the Helm chart. The config knob the ticket asks for exists as `jobs.queue_reconcile` with its own reasoned doc comment, so an operator can go further without a rebuild.\n\n`DefaultQueueWorkers`' comment narrates the change: \"It was 2, which is ~30 tenants — below any multi-tenant install — and is now 8, which is ~120.\"\n\nRelated: `39e48e2` (closed) fixed a separate defect on the same table — `config.Default()` and the Helm chart were overriding six of the eight published widths, so the §G.3 table described no process anybody ran. `reconcile` was the one row that already agreed.","files":null}]}