{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1787124069,"nonce":"Q19FvbnzBwcUg9UELjMBzS6hXcY=","message":"**Two tickets cut for scope this one should not absorb: `bd0fb1d` and `7287b28`.**\n\n- **`bd0fb1d`** — remove `unacked_reminder` (owner ruling). **Land it BEFORE stage 4.** Doing so dissolves the latch problem instead of making stage 4 solve it: `reminders.go:115-127` keys the once-per-generation latch on the literal `(alert_group, group_id)` and its own comment says moving that allocation unlatches it silently. Deleting `alert_groups` moves it by definition, and the ruled replacement is WORSE than the old key — a latch on a conversation that is stable per Case fires once and then suppresses every later incident in it, a silent MISS rather than a silent nag.\n- **`7287b28`** — the rollover rule. This ticket's done-when 5 (`channel_threads` gains `generation`) is where the gap lives: the justification given is channel archival, and **nothing says what advances the number** — `generation + 1` appears nowhere in the tree. Adding the column without a trigger yields a constant. Also records that this ticket's premise for it is false: the generation is NOT smuggled inside `subject_id`, it is plain row identity (`00008:15,46` · `00011:155`), verified twice.\n\nNeither depends on the other landing first. Both are referenced from the run handoff at `~/.afk/oto/briefs/handoff-2026-08-19-afk.md`.","files":null}]}