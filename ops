{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1786429058,"nonce":"ulQFnmLR3FK/in0rhb+YRFFnjRU=","message":"Done. Tests added: domain/fingerprint_stability_test.go, service/capture_test.go, service/matching_test.go, service/drift_db_test.go (harness-backed).\n\nCorrection: internal/rules was NOT untested — 9b88a50 added domain/{snapshot,diff,history}_test.go and 63dc4b6 added api/map_test.go, and snapshot_test.go already pins the §C.6 wire format. The real holes were service/ and repository/ (zero tests), no DB-backed drift test, and nothing distinguishing fractional for:.\n\nStability holds for every non-semantic difference tried — label/annotation order (7 names x 50 shuffles), duration spelling (600/600.0/6e2), nil vs empty maps, whitespace around expr, and all provenance fields — with ONE exception: IEEE-754 negative zero. nonNegative rejects f \u003c 0 and -0.0 \u003c 0 is false, so -0.0 renders '-0' and `for: -0` and `for: 0` are two content addresses. Pinned as a KNOWN WART. All 14 semantic mutations produce distinct addresses, including a key moved from labels to annotations, and fractional for: values are now pairwise distinct through both NewSnapshot and Postgres.\n\nmatch_confidence semantics documented from rulematch/resolve.go, including the non-obvious ones: 0 candidates WITH a generatorURL is exact/1 (the expression is what the alert reported, not a guess); a single candidate with a non-templated label mismatch downgrades to probable rather than being vetoed; N candidates with a clear winner still reports candidate_count=N; and expression equality is a HARD FILTER that collapses the count to 1, not a tiebreak.\n\nConstructible-invalid verdict: constructible YES, persistable NO — Service.Capture rebuilds an invalid snapshot as unavailable, and Upsert calls Validate() itself. Verified against real Postgres for four violations: refused in Go, no 23514, zero rows.\n\nTWO REAL DEFECTS FOUND, pinned by tests and filed separately: every unrecoverable rule in a source shares ONE row (the fingerprint covers the definition only, and every unavailable capture hashes identically); and the rule key splits when Prometheus becomes reachable, hiding a real edit.","files":null}]}