{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1787109371,"nonce":"DxiYRYhtQVOpChydxahkiGips8Q=","message":"DONE, and its last open clause is settled by the owner's ruling on `752cb18` (closed).\n\nClauses 1-5 landed in migration 00057: `case_policy_config` on namespace x alertname, the retention window W, a delayed close that is never a reopen, W defaulting to 0 as byte-for-byte today's behaviour, and ADR 0041 restating the Case.\n\nClause 6 required `notifications.suppressed_reason` to stop admitting `flapping` (done) and ended \"`is_flapping` remains visible\". It is visible in the letter — the column reads and `?flapping=` still filters — but FROZEN: nothing recomputes it, so the payoff this clause was written for (an operator sees it and goes to fix a Prometheus rule missing a `for:`) is unreachable.\n\nThe owner ruled the flap detector is not needed, so visible-but-frozen is accepted. Recorded in the schema by `062087f` (migration 00062), which comments both flap columns as retired in place.","files":null}]}