{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1787109070,"nonce":"36JKCUUZcUKvVbU9e/I57rGMvaU=","message":"Fixed in `069555b` on branch `case-and-grouping` (not pushed).\n\n`validateDigest` consults `MinDigestFloor`. The bound could not be enforced on the merged policy -- `Digest.Floor` spends zero on \"unset\", so an explicit 0 and an explicit null fold to the same value -- so the check moved to the patch, in `PolicyPatch.ValidateExplicit`, where the two are still distinct. An explicit null still clears the floor, with a test pinning that.\n\nGates run and recorded under `~/.afk/oto/20260819-0028/`. Closed by the AFK orchestrator session on the user's instruction.","files":null}]}