{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":1,"timestamp":1786481691,"nonce":"PHvbLuwwOHdjLKmrqWMKwZk7nnk=","title":"**What is wrong.** `docs/design/SPEC.md:5071` opens *\"### M.7 Enforcement\"* with a two-column table naming six tests. Zero of them exist, and nothing in `.github/workflows/ci.yml` runs anything equivalent. AC-44 through AC-48 (`SPEC.md:3869-3878` — themes, axe contrast, colour-blind legibility, Tier-B hue containment, the Slack hexes) are backed entirely by this table.","message":"**The demonstration.** Row by row, against the tree:\n\n- `web/src/design/contrast.test.ts` (SPEC.md:5075) — `web/src/design/` contains exactly three entries: `.gitkeep`, `theme.ts`, `tokens.css`.\n- `web/src/design/tokens.test.ts` (SPEC.md:5076) — same directory, same three entries.\n- `TestNoStateHueInChrome` (SPEC.md:5077) — the identifier appears nowhere in the repository outside that line.\n- `TestSlackPaletteUnchanged` (SPEC.md:5078) — appears nowhere outside `SPEC.md:5068` and `SPEC.md:5078`. `internal/channels/render/slack/palette.go:38-50` returns the six §H.2 hexes (`#a30200`, `#daa038`, `#dddddd`, `#2eb886`, `#6b6b6b`, `#7b1fa2`) and no test pins any of them.\n- Playwright `a11y.spec.ts` (SPEC.md:5079) — `web/e2e/` exists in the tree and holds exactly one file, a 0-byte `.gitkeep`. Neither `playwright` nor `axe` appears in `web/package.json` (dependencies or devDependencies), in `justfile`, or in `.github/workflows/ci.yml`. There is no e2e script to run a spec into: the `ui` job runs `npm ci`, `generate:check`, `gen:validators:check`, `typecheck`, `build` and `test` (vitest), and `web/package.json`'s own `verify` script is those same five steps.\n- `prefers-reduced-motion` snapshot (SPEC.md:5080) — no such test.\n\nThe one line in the tree that asserts any of this is a comment rather than a check. `web/src/design/tokens.css:12` reads *\"(§M.6): no `#a30200`-family literal may exist anywhere under web/.\"* `SPEC.md:5068` claims *\"a lint rule forbids Slack hex literals in `web/`\"* — no such rule exists in `.golangci.yml` or under `tools/`.\n\n**Why it matters.** README.md:128-129 states the standard in the repo's own voice: *\"Enforcement is worth stating precisely, because this repository has previously claimed gates it did not have.\"* This is the third instance of the identical failure mode; tickets 78d8553 and 5bc341a each closed a gate table nobody could fail. A table of six named test files is *worse* than silence, because a reader greps for `contrast.test.ts`, finds the §M.7 row, and stops looking. `web/e2e/` holding only a `.gitkeep` is the same trap in directory form.\n\n**The fork, stated honestly.** Two resolutions, and they are not the same size. Implement the table: `tokens.test.ts` and `TestSlackPaletteUnchanged` are each small, over data that already exists in `tokens.css` and `palette.go`; `TestNoStateHueInChrome` and the `prefers-reduced-motion` snapshot are moderate; `contrast.test.ts` needs the §M.4/§M.5 pairs transcribed as fixtures. The Playwright row is the bulk of the cost on its own — a new runner, a new CI job, a browser in CI, and a served build to point it at. Or reduce §M.7 to the rows that will actually run and move the rest into §N's amendment procedure as scheduled work, deleting `web/e2e/` with them. Either is defensible; leaving the table as it stands is not.\n\n**Where.** `docs/design/SPEC.md:5071-5080` (the table), `docs/design/SPEC.md:3869-3878` (AC-44..AC-48 that depend on it), `web/src/design/` (two missing tests), `web/e2e/` (`.gitkeep` only), `web/package.json` (no playwright, no axe, no e2e script), `.github/workflows/ci.yml` (the `ui` job), `internal/channels/render/slack/palette.go:38-50` (unpinned hexes), `web/src/design/tokens.css:12` (a comment standing in for a lint rule).\n\n**Scope.** medium\n\n**Done when.** Every row of §M.7 either names a test that exists and is invoked by `.github/workflows/ci.yml`, or is gone from the table. If `web/e2e/` still holds nothing but `.gitkeep` when this closes, §M.7 must not name a Playwright spec and the directory should go too. Each test that lands is demonstrated to fail against a planted violation, per README.md:144-147.","files":null}]}