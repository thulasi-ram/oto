{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1787116019,"nonce":"C0YEEstkf97xFP+VQVVoxLeOL4U=","message":"⛔ **DECIDED BY THE OWNER, 2026-08-19: `frozen` is DROPPED.**\n\nThis ticket asked for one of two outcomes — freeze on group-close, or remove `frozen` from the state vocabulary. The owner ruled: remove it.\n\nThat resolves the entanglement this ticket flagged. `7570090` is also being re-scoped so that a conversation is one Case (never a collapse of several), and the same ruling drops `unacked_reminder`. A state meaning \"the group closed\" has no trigger left once `alert_groups` is gone and a conversation is a Case, so wiring it would have been building a trigger for a concept that is being deleted.\n\n**Done when** (replacing the ticket's two-branch version): `frozen` leaves the `channel_threads.state` vocabulary, `Freeze` and `freezeThreadSQL` and the `ThreadStore.Freeze` port method go with it, the two tests that were its only callers go, and a new migration corrects `00011:197`'s column comment — which is the sentence that promised a state nothing could enter.","files":null}]}