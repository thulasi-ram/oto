{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":6,"timestamp":1786210148,"nonce":"giX8EYItKCSxMUI+3rQt3Yp0JSs=","target":"edb670f72b1956ee71c68b62098bbd5574cd1b816394b211649bddbf344821f4","message":"**What is wrong.** No Slack credential has ever been used by this codebase. The\naudit ran with `OTO_SLACK_ENABLED=false` and recorded ADR 0008 as\n\"**CONFORMS (static)** … *UNCONFIRMED at runtime* — Slack is disabled and I did\nnot stand up a fake Slack\" (`review-conformance.md`, ADR table). The Block Kit\ncard in `internal/channels/render/slack/` has never been rendered by Slack itself;\nit has only been checked against oto's own outbound validator V0–V18\n(`internal/channels/render/slack/validate.go`).\n\n**Why it matters.** Slack-first is the product's positioning. Block Kit fails in\nways a local validator cannot see: block/attachment nesting rules, text length\ntruncation, mrkdwn escaping, emoji resolution, button styles, and above all the\nthreading model — `thread_ts`, `reply_broadcast`, and `chat.update` on a root\nposted by a different token. If any of that is wrong, every card oto has ever\n\"delivered\" is wrong. ADRs 0008 (update-in-place) and 0020 (broadcast) are both\nuntested claims about Slack's behaviour.\n\n**Where.** `internal/channels/providers/slack/`, `internal/channels/render/slack/`,\n`deploy/slack/manifest.yaml`, `docs/setup/slack.md`.\n\n**Done when.** A real workspace has been connected from `deploy/slack/manifest.yaml`,\nand someone has observed, in a Slack client: a root card, a threaded reply, an\nin-place `chat.update` of the root, a broadcast reply appearing in-channel, an\n`@`-mention that actually notifies, and the resolved/snoozed variants. Screenshots\nor a golden capture land in the repo, and any renderer corrections are made.\n`docs/setup/slack.md` is updated with anything that turned out to be untrue.","files":null}]}