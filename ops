{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1787155476,"nonce":"OaGfr+vHBFOFpPDiX0ql7cetgUg=","message":"**Decided, 2026-08-19 — see `893cee4` comment #2.** Bounded lookback plus a reconciliation detector, not the `now − safety_lag` cursor comment #1 recommended. This ticket is fixed by that shape rather than on its own: a straggler lands in the next digest instead of falling below a frozen cursor, and dedupe state spans only the lookback rather than all of history. One thing to confirm before sizing the lookback: which clock stamps the timestamp it compares against — a transaction-scoped `now()` returns transaction START time, so it under-reports lateness for exactly the slow-committing Cases that matter.","files":null}]}