{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1786428877,"nonce":"K48A2byEyspsYjuA2qEsMMFrYhI=","message":"Fixed in 00034_app_clock_remaining.sql. Dropped 20 columns across 13 tables, each verified against every writer first.\n\nKept deliberately (9 columns / 6 tables), because a live writer omits them today: alerts, alert_occurrences, alert_groups (INSERTs never name them, UPDATEs write now() — internally consistent on the DB clock, and moving them means moving both directions together); alert_event_keys.created_at (TWO WRITERS DISAGREE — notification/repository/events.go names it, alerts/repository/event.go omits it; dropping would 23502 the ingest path); ui_events.at (the partition key, deliberately left to the DB); alert_snoozes.created_at (snooze.go stamps snoozed_at instead). A keptDefaults() assertion pins the six exceptions so a follow-up cannot 'finish the job' and break ingest.\n\nCount correction: the issue says sixteen tables; the real number was nineteen.","files":null}]}