{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1787109371,"nonce":"sAsSuzF3/EaqJJW/LcGqBm80pCE=","message":"DONE, with clause 3 superseded — recorded here so the supersession is not invisible.\n\nClauses 1, 2, 4 and 5 hold: both CHECKs widened and `notifications_subject_ck` ties `subject_id` to the matching id column (00056); `Reason.Subject()` exists and `reason_test.go:40` asserts the mapping is TOTAL; `threads_subject_uniq` is unchanged and both 00056 and 00058 state so explicitly; `ReasonEnriched` and `ReasonRuleChanged` are both allocated `SubjectCase` deliberately.\n\n⚠️ Clause 3 said \"`group_id` stays NOT NULL\". Migration 00058:276 does `ALTER TABLE notifications ALTER COLUMN group_id DROP NOT NULL`, because a digest (`7178492`) has no group to point at. Two tickets collided and the later one won; nothing in either recorded it until now. The outcome is almost certainly right — a digest genuinely has no delivery target — but it is a decision, not a formality, and this comment is where it is written down.","files":null}]}