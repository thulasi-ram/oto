{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1786944263,"nonce":"qOQByTDsBi5CzKSHzLh5Ge9DwGU=","message":"Done. `createSource` returns `Promise\u003cSourceCreated\u003e`; the `as unknown as SourceCreated` at the call site is gone, so the generated types cover that path again. `TokenDialog` renders `webhook_url` beside the token, each copyable on its own, guarded by `Show` because the field is optional in the contract. Both values go through a new `OneTimeSecret`, which reports a failed clipboard write instead of swallowing it — the old `void navigator.clipboard?.writeText(...)` was silent in exactly the cases where the secret is then lost.","files":null}]}