{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1787216322,"nonce":"e4FVzY5YlUUkw/Ab8P4hjMCqubY=","message":"Closed as **superseded by `7570090`**, per `b1e26f7`'s done-when.\n\nThe rename this ticket asks for has no object left to rename. `alert_groups` is deleted root and\nbranch (migration `00069_a_conversation_is_a_case.sql:298` `DROP TABLE alert_groups`), the\n`internal/grouping` package is gone, and `test/arch/sqltables_test.go:151` — where the\n`alert_groups` ownership row lived — is now a tombstone comment naming this same ticket pair.\n\nRecording the two corrections `b1e26f7` established, so they are not re-derived if this ever reopens:\n\n- Done-when 6 was factually wrong. It named `test/scope/allocation_test.go`, which contains zero\n  occurrences of `group` (re-verified: `grep -c group` returns 0). The arch test that was actually\n  affected is `test/arch/sqltables_test.go` at `:151`, `:176`, `:359`, `:536`.\n- The naming panel's finding is preserved for the record: `notification_groups` recommended,\n  `alert_conversations` runner-up — on the assumption the object remains the notification unit.\n  It no longer is, which is why the rename is moot rather than merely deferred.\n\n`7570090` was completed, not declined, so the supersession is final rather than conditional.","files":null}]}