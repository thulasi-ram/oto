{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1786281609,"nonce":"PnXdOiPeeehwfM9q/Nwia0VPHGs=","message":"Both halves addressed.\n\n**Orphaned containers** — closed by the test harness (14ad668). `test/harness` now brings up ONE container per test binary, migrates it once into a template, and gives each test its own database via `CREATE DATABASE ... TEMPLATE`. `test/integration` went from 14 containers to one and runs in ~17s. Schema-per-test was attempted first and is impossible here: `00005_partitions.sql` hard-codes `public.` inside `oto_partitions_manage()`, so partitions would be created in `public` while tables lived elsewhere. Truncate-between-tests was attempted second and deadlocked against `notification/service`'s `t.Parallel()`.\n\n**Undocumented environment** — closed by 5437438. `just test` now detects the colima socket and exports `DOCKER_HOST` plus `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE`, announcing what it did on stdout. Detected rather than hard-coded, so a pre-set `DOCKER_HOST` and Docker Desktop are both left alone. Note the working override on this machine is `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock` rather than the `TESTCONTAINERS_RYUK_DISABLED=true` the ticket names.\n\nThe three `main_test.go` bootstraps the ticket lists as separate sites no longer exist; they all go through the harness now.\n\nReady to close.","files":null}]}