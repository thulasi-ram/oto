{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":2,"timestamp":1786504425,"nonce":"5ZigBeU6Iew60PQ9RJ7N5UwM770=","title":"README's Layout section calls `test/contract/` and `test/load/` empty twelve lines above listing them as enforced","was":"**What is wrong.** README.md:120-123 says: *\"test/{contract,load}/  ⚠️ still a `doc.go` and no test. `test/load/` in particular claims the 5 000-alert storm batch that CONTEXT.md §6 says to write BEFORE the feature handling it. Treat these two as a plan, not as coverage.\"* README.md:137-138 then lists G1 as **enforced** via `go test ./test/contract/` and G2 as **enforced** via `go test ./test/contract/server/`. Both statements are on the same page, twelve lines apart."}]}