{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1786428922,"nonce":"UJ2DC3BeL84rcK7V08t+S3zRkXM=","message":"MOSTLY STALE — the wiring was already done in 889be65, verified against 889be65^ (which matches this issue's text exactly, including the lying comment). container.go has said Interactions: c.SlackInteractions since then. Four of the five acceptance criteria were already met end to end: the block action reaches the ack service via a River job, the Slack user is resolved and upserted into slack_identities, the card is amended in place through notify.evaluate(reason=acked) -\u003e ModeUpdateRoot, and test/integration/slack_acknowledge_test.go drives a signed payload with a real route table and real Postgres.\n\nThe one REAL gap, now closed: SPEC §H.8 requires an unknown action_id to emit slack_unknown_action_total. The code logged at warn and emitted nothing — and the 200 is mandatory (Slack disables event subscriptions when \u003e95% of deliveries fail in an hour), so an unroutable press was otherwise invisible from outside. Added internal/channels/service/metrics.go with the counter deliberately UNLABELLED (action_id comes from a payload oto does not author, i.e. unbounded TSDB cardinality; the id stays on the warn log), incremented in both Handle and Apply, plus tests asserting it fires for unknown/foreign ids and stays still for oto.ack and oto.noop.*.\n\nCorrections to the issue: the endpoint is /api/v1/**integrations**/slack/interactions, not /channels/; the line numbers are from 889be65^; and 'no ack written' was wrong even then — writeSlackAck did write a valid ack, which is exactly what made the failure silent.","files":null}]}