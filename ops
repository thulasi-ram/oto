{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":2,"timestamp":1786531128,"nonce":"4xVvOPkA3c+Qr+nI/kJ/hq1wytM=","title":"web/src/routes/alerts.tsx resets its keyset cursor one tick too late, so a filter change sends a request the server must refuse","was":"**What is wrong.** `web/src/routes/alerts.tsx` holds the keyset cursor and the current filters as two separate signals, reset in two separate `createEffect`s (`:99-109` for the filter fingerprint, `:111-118` for the roll-up axis). Both effects exist to keep a cursor from outliving the filters it was minted under — the file's own comment at `:94-97` says why: *\"Any filter change invalidates every cursor minted under the old filters (§E.3 answers a stale one with `400 cursor_filter_mismatch`), so the stack resets.\"* But `createEffect` runs too late to prevent that request from being built. solid-query's observer reads the query key from a `createComputed` — `web/node_modules/@tanstack/solid-query/src/useBaseQuery.ts:361-370`:"}]}