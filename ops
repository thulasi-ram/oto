{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1786484098,"nonce":"GR9S4sm52GVDH1bIsn3Yf5WdjUI=","message":"Fixed in cd5b10a. `deleteSource` now runs `SoftDelete` and `RevokeIngestTokens` inside `rt.inTx`, soft-delete first so the 404 for an unknown id still costs no write against `api_tokens`. The tests assert both writes ran on the transaction-marked context rather than that something committed, and `fakeTx.InTx` mirrors `db.TxOptions`' nesting short-circuit so a nested `inTx` cannot make the assertion pass for the wrong reason.","files":null}]}