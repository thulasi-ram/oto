{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":6,"timestamp":1786210150,"nonce":"8ILDFA1fw9+4UsghILx/VWMWErI=","target":"60d768ddf1eba3cad8c7db17623fcd827f0fa8464bc957843ab83b7c1acebc9c","message":"**What is wrong.** `web/src` contains one test file, `lib/matchers.test.ts`. The\nwhole SolidJS application — the API client and its valibot parsing, the SSE\nsubscription and its reducers, the alert list and its filters, alert detail, the\nack/snooze/comment actions, and the settings screens including the tuning\nevaluator — has no test at all. The CI `ui` job runs `npm run test`, which until\nrecently exited 1 because it found no test files (`review-conformance.md` D-2).\n\n**Why it matters.** The UI is the product for anyone who is not an Alertmanager\noperator, and it has drifted from the server before without anyone noticing:\n`PoliciesSection.tsx` offered a notification reason the server rejects with 422\nand could not label three it accepts, so an existing policy rendered `undefined`\nfor its reason chip (`review-conformance.md` S-3). A UI that silently renders\n`undefined` or drops an SSE event is indistinguishable from a working one until a\nuser is looking at a real incident.\n\n**Where.** `web/src/api/`, `web/src/features/`, `web/src/lib/`,\n`.github/workflows/ci.yml` (`ui` job).\n\n**Done when.** The API client, the SSE reducer (including reconnect and\nout-of-order events), the alert list filters and the ack/snooze flows have\ncomponent tests; the settings screens are tested against the server's real bounds\nand enum values rather than hand-copied lists; and the `ui` job's `npm run test`\nfails when they break.","files":null}]}