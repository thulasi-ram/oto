{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1787222732,"nonce":"3ZBydMbPzm+zxL5e/OOeOL0KjFk=","message":"**Not done, and not doable from code — recording that explicitly so this does not read as overlooked.**\n\nThe other nine tickets open on 2026-08-20 were closed in this pass. This one was triaged and then\ndeliberately left: its done-when requires a human driving a real Slack workspace and capturing what\nactually renders. No source change satisfies it, and there is no capture file anywhere in the repo.\n\n**Still owed, all four on the default path** (broadcast is tombstoned at\n`internal/channels/domain/ports.go`, so nothing here depends on it any more): the root card, the\nthreaded reply, an in-place `chat.update`, and the resolved/snoozed variants, plus the client-parity\nunknown in ADR 0020. Captures land in the repo and `docs/setup/slack.md` gets updated.\n\n⛔ **Two of this ticket's clauses are now permanently unanswerable and should be struck at the next\nedit**, rather than left to look like outstanding work: the `@`-mention notification and the\nsection-block-in-broadcast case both died with Slack thread-broadcast (git-bug `7570090`).\n\n⭐ **One thing changed in this pass that affects the capture list.** The digest now has its own Slack\nlayout (`78388fb`) and the snooze pair is now a real `static_select` in the action row (`0a8ca4a`,\nADR 0043 — the row renders five elements now, not four). So a verification pass should capture the\ndigest card and the snoozed/unsnoozed action row too; neither existed when this ticket was written.\n`test/harness/slack_cards.go` has no Block Kit Builder entry for the digest.","files":null}]}