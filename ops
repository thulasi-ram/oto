{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1786429057,"nonce":"c4lTEHvWNBsFk8yT3pOsp7hQTUU=","message":"Done. deploy/prometheus/prometheus.yml + oto-rules.yaml (the path SPEC §H.8:4614 already named, so no amendment needed) scrape oto's /metrics and route to the compose Alertmanager, with an always-true rule that fires within a minute. A prometheus service was added to docker-compose.yml — note `just infra` names services explicitly, so --wait would have skipped it; the recipe now lists it. promtool check config, amtool check-config and docker compose config all pass.\n\ndocs/runbooks/: 35 pages plus an index, one per registered oto_* collector — the grep found 35, one more than the four metrics.go files suggest. 21 judged alertable (4 of which the code itself labels ALERT ON THIS or 'this counter is an alert, not a statistic'), 11 informational with a page saying so and why (oto_ingest_duplicate_total being non-zero IS healthy under HA Alertmanager), plus oto_ingest_accepted_total alerted on ABSENCE.\n\nA real bug found and fixed on the way: deploy/alertmanager/alertmanager.yml posted to /api/v1/ingest/alertmanager with NO {source_id}. The route is /ingest/alertmanager/{source_id} and authenticates with that source's own token, so THE SHIPPED CONFIG COULD NEVER HAVE DELIVERED ANYTHING. It now carries an all-zero placeholder that 404s honestly, plus the exact POST /api/v1/sources call to get the real webhook_url and ingest_token.\n\nAll four empty .gitkeep directories deleted (deploy/prometheus, docs/runbooks, docs/adr, deploy/helm/oto/templates); no empty directories remain.\n\nReported separately: SPEC AC-34/§H.8/openapi.yaml name EIGHT metrics no collector constructs. The index has a table of where each fact actually lives today instead of inventing pages for them.","files":null}]}