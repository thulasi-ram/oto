{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1787077991,"nonce":"/ieKPHU7Zy0BtfxJczxBjkNKbuE=","message":"**Superseded by `7570090`** — `alert_groups` is being DELETED, not renamed, so this rename should not\nbe started. Not closed here; the owner closes tickets. The companion record is `b1e26f7`.\n\n`7570090`'s finding: `alert_groups` stores a grouping decision that is a pure function of four fixed\nlabels (`db/migrations/00050_derived_group_key.sql:143` — *\"FIXED, NOT CONFIGURABLE\"*), which makes it a\nnotification-policy decision wearing an entity's clothes. `group_by` moves to a\n`notification_policies` column and the collapse key is computed at delivery. `digest` already works\nthis way with no group row at all (`00058:124` keys a conversation `(channel_id, 'digest', policy_id)`).\n\nDone-when 2 above already anticipated this — *\"until then it is both a grouping of alerts and the\nnotification unit, which is why no name fits\"* — and `7570090` is the third branch it did not\nenumerate: the conversation key moves to the policy and nothing is left over to name.\n\nTwo corrections to this ticket, both carried into `b1e26f7` so they are not re-derived:\n\n- **Done-when 6 is wrong.** `test/scope/allocation_test.go` contains **zero** occurrences of\n  `alert_group`, `AlertGroup`, or even the bare substring `group`. The arch test that IS affected is\n  `test/arch/sqltables_test.go`, at `:151`, `:176`, `:359` and `:536`.\n- **\"94 non-test files in total\" in Where is low.** Measured on `case-and-grouping`:\n  `git grep -o -E \"alert_groups|AlertGroups|AlertGroup|alert-groups|alert_group\"` returns **961\n  occurrences across 175 files, 139 of them non-test**, with **38 sites** in `api/openapi/openapi.yaml`\n  across nine `/api/v1/alert-groups*` paths.\n\nAlso, done-when 3 is now partly done for a different reason: four of the six `group.*` event types\n(`member_joined`, `member_left`, `storm_started`, `storm_ended`) are already in `retiredEventTypes`\n(`internal/alerts/domain/event.go`). Only `group.opened` and `group.closed` are still appended.\n\nNaming panel finding, recorded so it is not re-convened if `7570090` is ever declined:\n**`notification_groups`** recommended, **`alert_conversations`** runner-up.","files":null}]}