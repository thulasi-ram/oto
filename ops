{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1787110317,"nonce":"3uF7IGeLDBjG8emH+u7LYG42qKU=","message":"Fixed in `4d9a39b`, pushed to `main`.\n\nAll three `guide` closures now return one verdict about the RETIREMENT rather than about the value, at level `inert`, with no `suggest`. They read nothing off the Alertmanager reference — there is no arithmetic left to argue from.\n\nThree `describe` blocks (324 lines) went with the mechanism they tested.\n\n⛔ The cross-knob invariant — \"agrees with flap_threshold exactly, because they are one inequality solved two ways\" — is DELETED, not moved. It existed because the pair once disagreed (`flap_window_s` carried an extra x 2 and made the screen contradict itself in two adjacent rows), which was a real defect and the right guard for it. There is no inequality left to solve two ways, and keeping it would pin a dead detector's arithmetic and make removing the keys harder.\n\nTwo set-level tests gained the three keys as explicit exceptions rather than being loosened: the unread-config test asserts they DO answer (same verdict whether or not Alertmanager is reachable), and the shipped-default test asserts `inert` rather than `ok`, because inert is honest at every value including the default.\n\n⭐ Still open: `27a1860` decides whether the three keys survive at all. If they go, this row disappears and this work goes with it — which is the right order, because the copy was actively false in the meantime.","files":null}]}