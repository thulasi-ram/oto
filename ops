{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1786484099,"nonce":"M1XH4/KUldXIkwTDctb9b6mr/t0=","message":"Fixed in 7c56bf5. `snapshotSource` deleted and the repository passed directly; its port-swap note re-homed rather than lost. `lateNotificationReader` also deleted — its only guard was `l == nil` and it was only ever constructed as a non-nil address. `groupVersions` was LEFT deliberately: its `g.svc == nil` branch is reachable between `alerts` being constructed and `grouping` being filled, and `grouping/service.StateVersion` has no nil-receiver guard, so collapsing it would turn 'version 0' into a panic. The asymmetry is now documented next to it.","files":null}]}