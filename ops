{"author":{"id":"5698b6d16d951325374257d86a2e673da0e55970c3d21c0f7c6f49018f24e5bf"},"ops":[{"type":3,"timestamp":1787032754,"nonce":"IEXTCNaf3cdv/EDBAKCaCoCWeog=","message":"Done in a5786e4, with one clarification. alerts.ack_state and alerts_ackstate_ck are dropped in 00049; the ?ack= filter and the acked/unacked roll-up counters are off the alert list.\n\nThe Done-when says the ack filter and count 'live on the Case surface instead'. They already did: GET /alert-groups?ack= and AlertGroupDTO.acked_count are sourced from the episode (grouping/repository/member.go:263). There is no org-wide case-list endpoint and building one is dc4d731's surface, so nothing new was invented — the alert-list versions were simply removed.\n\nNot named in the ticket but necessary: stats/repository/stats.go was a THIRD cross-module SQL read of alerts.ack_state (the dashboard overview). Redirected to join the current case, preserving today's numbers exactly. A cross-module SQL read has no compiler error, so this would have failed silently at runtime.\n\nTwo UI affordances were lost as a consequence and accepted deliberately: group-detail member rows lost their AckChip (listAlertGroupAlerts expands no episode), and GroupedAlerts lost its 'N unseen' chip (it read unacked_count). Restoring the first needs current_occurrence on that endpoint.","files":null}]}