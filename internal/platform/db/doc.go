// Package db owns the two pgx pools (ingest and general), the unit-of-work transaction helper, TenantScope, keyset pagination, LISTEN/NOTIFY, advisory locks, JSONB helpers, retry and the river enqueuer.
package db
