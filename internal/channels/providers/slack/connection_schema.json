{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://oto.dev/schemas/channel-connection/slack/v1.json",
  "title": "Slack connection",
  "type": "object",
  "additionalProperties": false,
  "required": ["team_id"],
  "properties": {
    "team_id": {
      "type": "string", "title": "Workspace ID",
      "pattern": "^T[A-Z0-9]{2,}$",
      "description": "Slack workspace id, e.g. T9TK3CUKW. Set up once per workspace; every channel under this connection posts into it."
    }
  }
}
